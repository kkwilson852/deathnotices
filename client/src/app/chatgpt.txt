I have the following call in the component to the service:

this.noticeEntryService.submitNotice(this.noticeEntryModel).subscribe(() => {
      this.isSubmitting = false;
    })

In the service, I have the following:

public submitNotice = (noticeEntryModel: NoticeEntryModel) => {
    return this.httpClient.post(this.apiUrl, fd).subscribe({
      next: (response) => {
        console.log('Notice submitted successfully:', response);
        return;
      }
    }
}