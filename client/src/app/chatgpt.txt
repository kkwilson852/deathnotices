<div class="card shadow-sm border-0 mb-4">
      <div class="card-body p-4">
        <div class="row justify-content-center">
          <div class="col-12 col-lg-8">
            <!-- Section Title -->
            <div class="mb-3">
              <h5 class="section-title mb-0">Personal Information</h5>
            </div>

            <!-- Desktop Header Row (like Contact section) -->
            <div class="row d-none d-md-flex mb-2">
              <div class="col-md-6">
                <label class="fw-bold">
                  Your Name <span class="text-danger">*</span>
                </label>
              </div>
              <div class="col-md-6">
                <label class="fw-bold">
                  Your Email <span class="text-danger">*</span>
                </label>
              </div>
            </div>

            <!-- Inputs Row -->
            <div class="row">
              <!-- Name -->
              <div class="col-12 col-lg-6 mb-3">
                <label class="d-md-none fw-bold">
                  Your Name <span class="text-danger">*</span>
                </label>

                <input
                  class="form-control"
                  [(ngModel)]="noticeEntryModel.buyer_name"
                  name="buyer_name"
                  type="text"
                  required
                  #buyer_name="ngModel"
                  (change)="saveNoticeData()"
                  [ngClass]="{
                    'is-valid':
                      buyer_name.valid &&
                      (buyer_name.dirty || buyer_name.touched),
                    'is-invalid':
                      buyer_name.invalid &&
                      (buyer_name.dirty || buyer_name.touched)
                  }"
                />

                <div
                  *ngIf="
                    buyer_name.invalid &&
                    (buyer_name.dirty || buyer_name.touched)
                  "
                  class="text-danger mt-1"
                >
                  <small>Name is required.</small>
                </div>
              </div>

              <!-- Email -->
              <div class="col-12 col-lg-6 mb-3">
                <label class="d-md-none fw-bold">
                  Your Email <span class="text-danger">*</span>
                </label>

                <input
                  class="form-control"
                  [(ngModel)]="noticeEntryModel.email"
                  name="buyer_email"
                  type="email"
                  required
                  pattern="^[^\s@]+@[^\s@]+\.[^\s@]{2,}$"
                  #buyer_email="ngModel"
                  (ngModelChange)="saveNoticeData()"
                  [ngClass]="{
                    'is-valid':
                      buyer_email.valid &&
                      (buyer_email.dirty || buyer_email.touched),
                    'is-invalid':
                      buyer_email.invalid &&
                      (buyer_email.dirty || buyer_email.touched)
                  }"
                />

                <div
                  *ngIf="
                    buyer_email.invalid &&
                    (buyer_email.dirty || buyer_email.touched)
                  "
                  class="text-danger mt-1"
                >
                  <small *ngIf="buyer_email.errors?.['required']">
                    Email is required.
                  </small>

                  <small *ngIf="buyer_email.errors?.['email']">
                    Please enter a valid email address.
                  </small>

                  <small *ngIf="buyer_email.errors?.['pattern']">
                    Email format is invalid.
                  </small>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>